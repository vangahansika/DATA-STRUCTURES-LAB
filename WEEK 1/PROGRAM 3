#include<iostream>
using namespace std;
int main()
{
   int a,b,c;
   cout<<"Enter a,b&c values:";
   cin>>a>>c;
   if(a>b&&a>c)
   cout<<"a is big";
   else if(b>c)
   cout<<"b is big";
   else
   cout<<"c is big";
}
